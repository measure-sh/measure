<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MeasureEnabledApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Measure">

        <profileable
            android:shell="true"
            tools:targetApi="29" />

        <activity android:name=".ComposeTargetFinderBenchmarkActivity" />
        <activity android:name=".ViewTargetFinderBenchmarkActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Measure">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="sh.measure.android.API_KEY"
            android:value="msrsh_key" />

        <meta-data
            android:name="sh.measure.android.API_URL"
            android:value="http://localhost:8080" />
    </application>

</manifest>