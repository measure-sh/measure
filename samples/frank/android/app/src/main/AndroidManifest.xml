<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.FrankensteinApp">
        <meta-data android:name="sh.measure.android.API_KEY" android:value="${measureApiKey}"/>
        <meta-data android:name="sh.measure.android.API_URL" android:value="${measureApiUrl}"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FrankensteinApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FlutterScreenActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ComposeScreenshotActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".ViewScreenshotActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".ComposeNavigationActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".FragmentNavigationActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".NestedFragmentActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".OkHttpActivity"
            android:theme="@style/Theme.FrankensteinApp" />
        <activity
            android:name=".ReactNativeScreenActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
