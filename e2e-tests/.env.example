# Runner config, loaded automatically. Copy to .env and fill in.
# The device a full run boots when none is attached (empty requires a running
# one). List names with `emulator -list-avds` / `xcrun simctl list devices`.
ANDROID_AVD=""
IOS_SIMULATOR=""
